let name = "Capivara",
traveledDistance = 300,
timeSpent = 10

function calculateSpeed(num1, num2){
    return num1 / timeSpent
}

console.log("velocidade média:", calculateSpeed(traveledDistance, timeSpent))